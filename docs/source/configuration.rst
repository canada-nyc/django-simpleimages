Configuration
===============================================

Add ``simpleimages`` to your ``INSTALLED_APPS``.

If you dont want to overwrite existing images, then set
``SIMPLEIMAGES_OVERWRITE`` to false.
